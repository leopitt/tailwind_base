/**
 * @file
 * Systemseed Tailwind Base theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.ssTwBase = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
